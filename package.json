{
  "name": "jd-analyzer",
  "version": "1.2.0",
  "description": "Privacy-first Chrome extension that scores resumes against job descriptions using your own Claude or OpenAI API key.",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.9"
  }
}
